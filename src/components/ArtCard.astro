---
import type { ContentCardModel } from '../lib/content';

interface Props {
  model: ContentCardModel;
}

const { model } = Astro.props;
const formattedDate = model.date
  .toLocaleDateString('en-US', {
    month: 'numeric',
    day: 'numeric',
    year: 'numeric',
  })
  .replace(/\//g, '.');
---

<a
  href={model.href}
  data-tags={model.tags.join(',')}
  class="post-item group content-card flex h-full flex-col overflow-hidden"
>
  <div
    class="content-card__image aspect-[4/3]"
  >
    {
      model.image && (
        <img
          src={model.image}
          alt={model.title}
          class="h-full w-full object-cover"
        />
      )
    }
  </div>

  <div class="flex flex-1 flex-col justify-between p-4 gap-3">
    <div>
      <time class="font-mono text-xs font-semibold text-slate-400">
        {formattedDate}
      </time>
      <h3
        class="mt-1 font-semibold text-base leading-tight text-slate-100 transition-colors group-hover:text-sky-200"
      >
        {model.title}
      </h3>
    </div>
  </div>
</a>
